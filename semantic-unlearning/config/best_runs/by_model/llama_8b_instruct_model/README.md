# Llama 8B Instruct Model — pending benchmark results

This directory is reserved for the next MCF, ZsRE, and TOFU runs on the selected 8B Instruct model.

Expected layout after evaluation:

- `mcf/`
- `zsre/`
- `tofu/`

Do not copy Llama 3B metrics into this directory. Add only results produced by the 8B checkpoint and record the exact model identifier, revision/hash, evaluator protocol, seeds, and checkpoint hashes.
