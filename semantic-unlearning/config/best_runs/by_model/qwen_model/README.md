# Qwen Model — pending benchmark results

This directory is reserved for MCF, ZsRE, and TOFU runs on the Qwen model selected for the cross-model study.

Expected layout after evaluation:

- `mcf/`
- `zsre/`
- `tofu/`

Before adding results, record the exact Qwen model identifier and revision. Add only metrics produced by that checkpoint, together with evaluator protocol, seeds, configuration, and checkpoint hashes.
