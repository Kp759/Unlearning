#!/usr/bin/env bash
set -euo pipefail

echo "V3.4 is a training-only primary cap sweep; checkpoint ablations are disabled." >&2
echo "Run scripts/submit_mcf_embedding_keyed_neuron_seed1.sh instead." >&2
exit 2
